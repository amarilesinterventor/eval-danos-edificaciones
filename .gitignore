node_modules/
dist/
data/*.sqlite
data/*.sqlite-journal
public/uploads/*
!public/uploads/.gitkeep
public/styles.css
*.log
.env
.DS_Store

# PDF de referencia usado solo para el análisis inicial (no lo lee el
# servidor en tiempo de ejecución) — se excluye del repo público por ser una
# publicación con derechos de autor de terceros (FOPAE-AIS, ~15MB).
/guia-patologias.pdf

# Copia de trabajo en la raíz (la copia que sí se versiona y se usa en
# tiempo de ejecución vive en assets/official-form/, ver más abajo).
/2A-Formulario regional homogenizado.pdf

# El formulario oficial fuente SÍ se necesita en tiempo de ejecución (base
# del PDF "formato oficial" — ver src/server/reportPdfOficial.ts) y es un
# formato gubernamental público, no una publicación con derechos reservados
# de terceros: vive en assets/official-form/ y SÍ se versiona.
